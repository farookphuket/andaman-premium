# andaman-premium
Andaman Premium - Qwen AI วินัย 


---

## 26 May 2026 📝 Dev Log: ฟอร์มจองทัวร์ (BookingTourForm)

| ปัญหา | ทางแก้ | รายการที่ทำ | สถานะ | log |
| :--- | :--- | :--- | :---: | :--- |
| **โครงสร้างข้อมูล / รหัสทัวร์ไม่ชัดเจน**<br>- หน้าบ้านไม่มีการส่ง `tour_id` แบบตัวเลขที่แน่นอน<br>- ส่งผลให้ Laravel Backend มองเห็นค่าเป็น Text String หรือตรวจไม่พบรหัสแพ็กเกจทัวร์ | **ปรับปรุงฟังก์ชัน Handle Submit**<br>- บังคับดึงรหัส `id` จาก Object ของทัวร์โดยตรง<br>- แปลงค่าให้อยู่ในรูปแบบตัวเลข (`Number`) ชัวร์ ๆ ก่อนทำการยิง API | - เพิ่มการตรวจสอบ `finalTourId` จาก `tour.id`<br>- ใส่ระบบตรวจสอบความถูกต้อง (Validation Checking) ป้องกันข้อมูลขาดหาย<br>- ครอบฟังก์ชัน `Number()` ในส่วน `tour_id` ก่อน Payload | 🟢 Done | ระบบรองรับการบันทึกข้อมูลและส่งต่องานไปยังหลังบ้าน (Laravel) ได้อย่างถูกต้องแม่นยำ ไม่พบปัญหา Invalid Tour ID แล้ว |
| **ระบบแจ้งเตือนสำเร็จแสดงผลไม่สวยงาม**<br>- หลังบ้านตอบกลับมาเป็น `success: true` บ้าง หรือ `status: "success"` บ้าง<br>- ฟอนต์และสีของ SweetAlert2 ยังไม่แมตช์กับดีไซน์ของเว็บ | **ปรับปรุงโค้ดตรวจสอบ Response และตกแต่งหน้าตา Alert**<br>- เขียนเงื่อนไขให้ดักจับสถานะได้ยืดหยุ่นขึ้น<br>- แต่งโค้ด CSS Inline ใน `Swal.fire` เพื่อคุมโทนฟอนต์และสีสัน | - แก้เงื่อนไขเป็น `result.status === "success" \|\| result.success === true`<br>- ปรับแต่งสีปุ่ม ตกแต่ง HTML ในส่วนข้อความ (Text) และเปลี่ยนดีไซน์ปุ่มเป็นสีเขียว Emerald (`#10B981`) | 🟢 Done | หน้าต่างแจ้งเตือนแสดงผลอย่างสวยงาม คุมโทนการดีไซน์ และรองรับคำตอบกลับจากเซิร์ฟเวอร์ทุกรูปแบบอย่างเสถียร |
| **สีของปุ่มหลักและ UI หลุดธีม**<br>- ปุ่มจองทัวร์, แถบ Progress Bar, และตัว Badge ด้านบน ยังใช้สีแดง (`bg-red-500`) ซึ่งไม่ตรงกับสไตล์หลักของเว็บไซต์ | **ทำความสะอาดโค้ดและปรับมาใช้ Utility Tailwind Theme**<br>- ค้นหาจุดที่มีการล็อกรหัสสีแดงตายตัว<br>- เปลี่ยนไปผูกกับค่าตัวแปรกลาง (`primary`) ของระบบแทน | - เปลี่ยนปุ่มหลักจาก `bg-red-500` เป็น `bg-primary`<br>- ปรับเปลี่ยนสี Badge "BOOKING FORM" ให้เป็นสีธีมร่วมกับค่าโปร่งแสง (`bg-primary/10`) <br>- เปลี่ยนแถบสเต็ปการจอง (Stepper Progress) ให้วิ่งตามแถบสีของธีมหลัก | 🟢 Done | ปุ่มและองค์ประกอบต่าง ๆ ทั้งหมดเปลี่ยนมาใช้สีตามระบบ Theme หลักของเว็บเรียบร้อย สวยงาม กลมกลืน ไม่หลุดธีมสีแดงแล้ว |

---

=========================================================================================
 [LOGO] ANDAMAN PREMIUM                    [ ทัวร์ภูเก็ต ] [ จองทัวร์ ]  [TH/EN/ZH] [🎨 สีธีม] 
=========================================================================================
 
   🌅 [HERO SECTION] 
   "สัมผัสประสบการณ์ทะเลอันดามันระดับลักชัวรี่ กับอันดามันพรีเมียม"
   (ข้อความนี้จะเปลี่ยนภาษาออโต้ตามที่เลือกด้านบน ส่วนปุ่มและสีพื้นหลังจะเปลี่ยนตามธีมสีที่กด)
   
   [ ปุ่ม: ค้นหาโปรแกรมทัวร์พรีเมียม ] -> (เปลี่ยนสีตามธีม เช่น ส้มทอง / น้ำเงิน / เขียว)

-----------------------------------------------------------------------------------------
 🏝️ [FEATURED TOURS SECTION] - โปรแกรมทัวร์แนะนำยอดฮิต
 
  +---------------------------+  +---------------------------+  +---------------------------+
  | [รูปเกาะพีพี อ่าวมาหยา]     |  | [รูปหมู่เกาะสิมิลัน]        |  | [รูปเกาะรอก ดำน้ำตื้น]      |
  |                           |  |                           |  |                           |
  | ทัวร์วันเดย์ทริป เกาะพีพี     |  | สิมิลัน พรีเมียม สปีดโบ๊ท    |  | เกาะรอก สวรรค์อันดามัน     |
  | ฿3,900 / ท่าน             |  | ฿4,500 / ท่าน             |  | ฿4,200 / ท่าน             |
  | [ รายละเอียด ] [ จองเลย ]  |  | [ รายละเอียด ] [ จองเลย ]  |  | [ รายละเอียด ] [ จองเลย ]  |
  +---------------------------+  +---------------------------+  +---------------------------+

-----------------------------------------------------------------------------------------
 📱 [FOOTER SECTION]
 บริษัท อันดามันพรีเมียม .Co., Ltd  | โทร: process.env.NEXT_PUBLIC_OWNER_MOBILE
=========================================================================================


