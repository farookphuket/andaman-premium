# andaman-premium
Andaman Premium - AI วินัย 

---
## 📝 28 may 2026 สรุปการทำงานและแก้ไขปัญหา (Project Log) โดยวินัย Gemini

| ปัญหา | สาเหตุ | วิธีแก้ไข | สถานะ | บันทึกเพิ่มเติม |
| :--- | :--- | :--- | :---: | :--- |
| **กองทัพเมนูบดบังสายตาบนมือถือ** | มีการใส่เมนูหลักซ้ำซ้อนกับแถบ `<BottomNav />` ด้านล่าง ทำให้รกจอโดยไม่จำเป็น | รื้อถอนปุ่มลิงก์เมนูย่อยด้านบนทิ้งทั้งหมด เพื่อเปิดพื้นที่ให้หน้าจอคลีนและสบายตา | ✅ สำเร็จ | หน้าจอฝั่งมือถือโล่งโจ้ง พรีเมียม และไม่สับสนเวลากดใช้งานครับป๋า |
| **เมนูเปลี่ยนภาษาจมหายใต้บาร์ล่าง** | ตัวกล่องเมนูอยู่ชิดก้นจอเกินไป พอแผง Dropdown ภาษาทิ้งดิ่งลงล่าง จึงโดนบาร์ล่างอุ้มหายมิด | ปรับลอจิกเป็น `justify-center` เพื่อดึงกล่องควบคุมขึ้นมาล็อกตำแหน่งไว้กึ่งกลางหน้าจอ | ✅ สำเร็จ | มีพื้นที่ด้านล่างเหลือเฟือให้ Dropdown กางปีกแสดงผลได้ครบทุกภาษา |
| **แผงเปลี่ยนธีมโผล่ซีกเดียวหลุดขอบ** | ใช้โครงสร้างแถวเดี่ยวที่แคบเกินไป (`max-w-sm flex`) ทำให้ Popover ธีมกางออกข้างแล้วทะลุขอบจอ | ยำโครงสร้างกล่องแม่เป็นแนวตั้งสองชั้น (`flex-col`) แล้วขยายเนื้อที่แกนกลางให้กางสวิตช์ได้เต็มเฟรม | ✅ สำเร็จ | พาเลทสีโหมดมืด (Midnight/Deep Blue) แสดงผลกลางจอชัดเจนแจ่มแจ้ง |
| **ปุ่ม Hamburger กลืนหายในโหมดมืด** | ตัวปุ่มเดิมไม่มีคลาสตัดสีและขอบที่คมชัดพอสำหรับพื้นที่หลังสว่าง/มืด | สาดสี Tailwind ชุดใหม่ (`dark:bg-slate-800 dark:border-slate-700`) ล็อกขอบเข้มสะดุดตา | ✅ สำเร็จ | ตอนเปิดเมนูปุ่มจะเปลี่ยนเป็น `X` โทนสี Rose อ่อนโยนเด่นหราสไตล์ตัวมัม |
| **ซาลาเปาลวกปาก (Hydration Error)** | ไฟล์ `[locale]/layout.tsx` ดันเสกแท็ก `<html>` ซ้อนอยู่ใต้แท็ก `<div>` ฟอนต์ Inter ของไฟล์นอกสุด | โยกแท็ก `<html>` และ `<body>` ไปคุมหัวท้ายที่ `src/app/layout.tsx` จุดเดียวตามกฎ Next.js แล้วถอนรากแท็กซ้ำซ้อนออก | ✅ สำเร็จ | คอนโซลใสสะอาดกริ๊บ สลับภาษา `TH/EN/CN` วิ่งฉลุยหัวแตกไม่มีบั๊กเด้งลวกปากป๋า |
| **ตัวหนังสือจม/อ่านไม่ออกใน Dark Theme** และปุ่มสลับภาษาไม่มีคู่สีมืด | ไฟล์ `globals.css` ฝั่ง `.dark` ไม่มีการแมพเปลี่ยนค่าตัวแปรสากลกลุ่ม `--color-muted`, `--color-muted-foreground` และกล่อง `--color-popover` ของระบบ **Tailwind v4** ทำให้ดึงสีของธีมสว่างมาใช้จนสีกลืนไปกับพื้นหลังสีน้ำเงินเข้ม | อัปเดตไฟล์ `src/app/globals.css` โดยเพิ่ม Semantic Tokens และคู่สีมืดที่จำเป็นเข้าไปในคลาส `.dark` ให้แสดงผลแยกกันเด็ดขาด พร้อมปรับระดับความโปร่งแสงของ Webkit Scrollbar | ✅ **เสร็จสิ้น** | อ้างอิงผลลัพธ์ความสำเร็จตามไฟล์ภาพ `002-edit-text-on-dark-theme_success.jpg` ตัวหนังสือและไอคอนแสดงผลคมชัดสว่างตาตามเกณฑ์ดีเยี่ยม |
| **หน้าหลัก (Home) แสดงการ์ดทัวร์เด่นคงที่เพียง 3 ใบ** และยังไม่มีระบบสุ่ม | โค้ดเดิมในไฟล์ `page.tsx` มีการจำกัด `limit: 3` และกำหนดค่า `sort: "popular"` แบบตายตัวมาจากระบบหลังบ้าน (Laravel) ทำให้หน้าเว็บดูซ้ำเดิม ไม่ดึงดูดสายตา | 1. อัปเดต `api.ts` ให้รองรับฟิลเตอร์ `is_featured`<br>2. ปรับปรุง `page.tsx` ให้ Fetch ทัวร์มาเผื่อเลือก 20 รายการ แล้วนำมาสุ่มสับตำแหน่งด้วยตรรกะ `Math.random() - 0.5` ก่อนจะหั่นแผ่นเค้กตัดเอาเฉพาะ 6 ใบพอดิบพอดีส่งลงหน้าบ้าน | ✅ **เสร็จสิ้น** | อ้างอิงตามไฟล์ภาพ `002_success_show-content.jpg` ระบบจัดเรียงตัวรูปแบบ Grid 6 ใบ (แสดงผล 3 คอลัมน์บาลานซ์สมมาตรพอดีบนเดสก์ท็อป) และจะสุ่มเปลี่ยนชุดทัวร์ใหม่ทุกครั้งที่มีการรีเฟรชหน้าเว็บ |
| **ระบบไม่จดจำหน้าล่าสุดของรายการทัวร์ (Lost Search State)** เมื่อเปลี่ยนหน้าไปยังหน้าอื่น (เช่น จาก `/tours?page=3` ไปยัง `/auth`) แล้วกดเมนูกลับมาตรง ๆ ระบบจะรีเซ็ตกลับไปหน้า 1 เสมอ | ลิงก์บนระบบนำทาง (Navbar) ถูกตั้งค่าเป็นสัญจรแบบ Static Link (`/tours`) โดยไม่มีกลไกในการจัดเก็บและสืบค้นประวัติพารามิเตอร์ล่าสุด (Query Params) ของผู้ใช้งานฝั่งบราวเซอร์ | 1. อัปเดตโครงสร้างหน้า `tours/page.tsx` เป็น Client-side เพื่อให้สามารถเข้าถึง `sessionStorage` และบันทึกค่า Query String ล่าสุดผ่าน Hook `useSearchParams`<br>2. ปรับปรุง `Navbar.tsx` ให้อ่านค่าความทรงจำล่าสุดมาประกอบร่างกลายเป็น Dynamic Link พากดวาร์ปกลับพิกัดเก่าแบบอัตโนมัติ พร้อมใส่ Logic ล้างไพ่เมื่อผู้ใช้ตั้งใจกดซ้ำ | ✅ **เสร็จสิ้น** | - ใช้ระบบ **SessionStorage** ทำงานบนบราวเซอร์ 100% ปลอดภัยและไม่กวนระบบหลังบ้าน<br>- ตัวแปรพารามิเตอร์ท้าย URL ข้ามฟาก (เช่น `callbackUrl=%2F...`) เป็นกระบวนการ URL Encoding ตามมาตรฐานสากล ปลอดภัยและไม่มีผลกระทบต่อประสิทธิภาพการทำงาน |


--- 
## 📝 สรุปการทำงานและแก้ไขปัญหา (Project Log) โดยวินัย Qwen

| ปัญหา | สาเหตุ | วิธีแก้ไข | สถานะ | บันทึกเพิ่มเติม |
|-- |-- |-- |-- |-- |
| **SEO Dynamic Title ไม่เปลี่ยน** | ยังไม่มีการตั้งค่า Metadata ตาม Locale | เพิ่ม `generateMetadata` ใน `[locale]/layout.tsx` และหน้า Page ย่อยๆ | ✅ เสร็จแล้ว | ใช้ `next-intl/server` ดึง Translation |
| **หน้า 404 (not-found.tsx) หายวูบ** | เกิด Hydration Error เพราะเป็น `"use client"` + มี Style ที่ซับซ้อน | เปลี่ยนเป็น **Server Component** (ลบ `"use client"`) และย้าย CSS Animation ไป `globals.css` | ✅ เสร็จแล้ว | ใช้ `suppressHydrationWarning` ใน Root ช่วยได้ |
| **เปลี่ยนภาษาแล้ว URL ผิดเพี้ยน (เช่น /th/en/en)** | Syntax error ใน `LanguageSwitcher.tsx` (มีช่องว่างในสตริง `"th "`) และการใช้ Operator ผิด (`& &`) | เขียนโค้ดใหม่ให้สะอาด (Clean Code) ลบช่องว่าง และใช้ `router.push(pathname, { locale })` | ✅ เสร็จแล้ว | ระวังเรื่อง Syntax error เล็กๆ ทำให้พังทั้งระบบ |
| **Hydration Error: Font mismatch** | โหลด Font `Inter` ซ้ำกัน หรือโหลดผิดที่ (Root vs Locale) | ย้ายการโหลด Font ไปที่ `src/app/layout.tsx` (Root Layout) เท่านั้น | ✅ เสร็จแล้ว | ใช้ CSS Variables (`--font-inter`) เชื่อมกับ Tailwind |
| **Route Conflict Error** | มีไฟล์ `[[...slug]]/page.tsx` ขัดแย้งกับ `page.tsx` ปกติ | ลบไฟล์ Catch-all ที่ไม่จำเป็นออก หรือย้ายไปไว้ถูกกลุ่ม (Route Group) | ✅ เสร็จแล้ว | Next.js 16 ไม่ชอบความกำกวมของ Route |
| **ข้อความบางส่วนไม่เปลี่ยนภาษา** | Hardcode ข้อความภาษาไทยลงไปในโค้ดโดยตรง | เปลี่ยนข้อความเป็น Key (เช่น `t("home.title")`) และเติม Key ในไฟล์ `messages/*.json` | ✅ เสร็จแล้ว | ตรวจสอบทั้ง `th.json`, `en.json`, `zh.json` |
| **Featured Tours เป็นข้อมูลจำลอง** | ยังไม่ได้ดึงข้อมูลจาก API จริง | เปลี่ยนจากการ Hardcode array เป็นดึงข้อมูลผ่าน `toursApi.getAll` ใน Parent Component | ✅ เสร็จแล้ว | ส่ง Props `tours` เข้าไปให้ Component แสดงผล |

---

### 💡 บทเรียนสำคัญ (Key Takeaways)
1. **Syntax Matters:** ช่องว่างเล็กๆ ใน String (`"th "`) หรือการพิมพ์ผิด (`& &` แทน `&&`) สามารถทำให้ Logic การเปลี่ยนภาษาพังทั้งระบบได้
2. **Server vs Client:** หน้าสำคัญๆ เช่น `not-found` ควรเป็น Server Component เพื่อป้องกัน Hydration Error และทำ SEO ได้ดีกว่า
3. **Clean Routing:** อย่าสร้าง Route ที่ซ้ำซ้อนกัน (Overlapping routes) ให้ Middleware หรือ `navigation.ts` จัดการ Locale prefix จะปลอดภัยที่สุด


---

## 26 May 2026 📝 Dev Log: ฟอร์มจองทัวร์ (BookingTourForm)

| ปัญหา | ทางแก้ | รายการที่ทำ | สถานะ | log |
| :--- | :--- | :--- | :---: | :--- |
| **โครงสร้างข้อมูล / รหัสทัวร์ไม่ชัดเจน**<br>- หน้าบ้านไม่มีการส่ง `tour_id` แบบตัวเลขที่แน่นอน<br>- ส่งผลให้ Laravel Backend มองเห็นค่าเป็น Text String หรือตรวจไม่พบรหัสแพ็กเกจทัวร์ | **ปรับปรุงฟังก์ชัน Handle Submit**<br>- บังคับดึงรหัส `id` จาก Object ของทัวร์โดยตรง<br>- แปลงค่าให้อยู่ในรูปแบบตัวเลข (`Number`) ชัวร์ ๆ ก่อนทำการยิง API | - เพิ่มการตรวจสอบ `finalTourId` จาก `tour.id`<br>- ใส่ระบบตรวจสอบความถูกต้อง (Validation Checking) ป้องกันข้อมูลขาดหาย<br>- ครอบฟังก์ชัน `Number()` ในส่วน `tour_id` ก่อน Payload | 🟢 Done | ระบบรองรับการบันทึกข้อมูลและส่งต่องานไปยังหลังบ้าน (Laravel) ได้อย่างถูกต้องแม่นยำ ไม่พบปัญหา Invalid Tour ID แล้ว |
| **ระบบแจ้งเตือนสำเร็จแสดงผลไม่สวยงาม**<br>- หลังบ้านตอบกลับมาเป็น `success: true` บ้าง หรือ `status: "success"` บ้าง<br>- ฟอนต์และสีของ SweetAlert2 ยังไม่แมตช์กับดีไซน์ของเว็บ | **ปรับปรุงโค้ดตรวจสอบ Response และตกแต่งหน้าตา Alert**<br>- เขียนเงื่อนไขให้ดักจับสถานะได้ยืดหยุ่นขึ้น<br>- แต่งโค้ด CSS Inline ใน `Swal.fire` เพื่อคุมโทนฟอนต์และสีสัน | - แก้เงื่อนไขเป็น `result.status === "success" \|\| result.success === true`<br>- ปรับแต่งสีปุ่ม ตกแต่ง HTML ในส่วนข้อความ (Text) และเปลี่ยนดีไซน์ปุ่มเป็นสีเขียว Emerald (`#10B981`) | 🟢 Done | หน้าต่างแจ้งเตือนแสดงผลอย่างสวยงาม คุมโทนการดีไซน์ และรองรับคำตอบกลับจากเซิร์ฟเวอร์ทุกรูปแบบอย่างเสถียร |
| **สีของปุ่มหลักและ UI หลุดธีม**<br>- ปุ่มจองทัวร์, แถบ Progress Bar, และตัว Badge ด้านบน ยังใช้สีแดง (`bg-red-500`) ซึ่งไม่ตรงกับสไตล์หลักของเว็บไซต์ | **ทำความสะอาดโค้ดและปรับมาใช้ Utility Tailwind Theme**<br>- ค้นหาจุดที่มีการล็อกรหัสสีแดงตายตัว<br>- เปลี่ยนไปผูกกับค่าตัวแปรกลาง (`primary`) ของระบบแทน | - เปลี่ยนปุ่มหลักจาก `bg-red-500` เป็น `bg-primary`<br>- ปรับเปลี่ยนสี Badge "BOOKING FORM" ให้เป็นสีธีมร่วมกับค่าโปร่งแสง (`bg-primary/10`) <br>- เปลี่ยนแถบสเต็ปการจอง (Stepper Progress) ให้วิ่งตามแถบสีของธีมหลัก | 🟢 Done | ปุ่มและองค์ประกอบต่าง ๆ ทั้งหมดเปลี่ยนมาใช้สีตามระบบ Theme หลักของเว็บเรียบร้อย สวยงาม กลมกลืน ไม่หลุดธีมสีแดงแล้ว |

---


 [LOGO] ANDAMAN PREMIUM                    [ ทัวร์ภูเก็ต ] [ จองทัวร์ ]  [TH/EN/ZH] [🎨 สีธีม] 

 
   🌅 [HERO SECTION] 
   "สัมผัสประสบการณ์ทะเลอันดามันระดับลักชัวรี่ กับอันดามันพรีเมียม"
   (ข้อความนี้จะเปลี่ยนภาษาออโต้ตามที่เลือกด้านบน ส่วนปุ่มและสีพื้นหลังจะเปลี่ยนตามธีมสีที่กด)
   
   [ ปุ่ม: ค้นหาโปรแกรมทัวร์พรีเมียม ] -> (เปลี่ยนสีตามธีม เช่น ส้มทอง / น้ำเงิน / เขียว)

-----------------------------------------------------------------------------------------
 🏝️ [FEATURED TOURS SECTION] - โปรแกรมทัวร์แนะนำยอดฮิต
 
  +---------------------------+  +---------------------------+  +---------------------------+
  | [รูปเกาะพีพี อ่าวมาหยา]     |  | [รูปหมู่เกาะสิมิลัน]        |  | [รูปเกาะรอก ดำน้ำตื้น]      |
  |                           |  |                           |  |                           |
  | ทัวร์วันเดย์ทริป เกาะพีพี     |  | สิมิลัน พรีเมียม สปีดโบ๊ท    |  | เกาะรอก สวรรค์อันดามัน     |
  | ฿3,900 / ท่าน             |  | ฿4,500 / ท่าน             |  | ฿4,200 / ท่าน             |
  | [ รายละเอียด ] [ จองเลย ]  |  | [ รายละเอียด ] [ จองเลย ]  |  | [ รายละเอียด ] [ จองเลย ]  |
  +---------------------------+  +---------------------------+  +---------------------------+

-----------------------------------------------------------------------------------------
### 📱 [FOOTER SECTION]
 บริษัท อันดามันพรีเมียม .Co., Ltd  | โทร: process.env.NEXT_PUBLIC_OWNER_MOBILE



